function [v] = FEuler(t_end, y_0, u,dt)
variables
%alpha = 10;
a(0)=0;
v(0)=0;
u(0)=0;
for t=1:t_end/dt
     switch v(t)
          case v(t)<v12&v(t)<alpha
            F=
            P=
            disp('X1')
          case v(t)<v12&v(t)<alpha
            
          case v(t)<v12&v(t)<alpha
            
          otherwise
            
        end
    v(t+1)=v(t)+dt*a(t);
    a(t+1)=1/m*(F-P)

    if v(t+1)<10^-3
        v(t+1)=0
    end
end
